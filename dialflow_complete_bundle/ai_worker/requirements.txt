# DialFlow AI media worker (Phase 2)
# Python 3.11+ recommended.

livekit-agents[sarvam,openai,google,silero]~=1.5
livekit-api~=1.0
httpx>=0.27
python-dotenv>=1.0

# Local embeddings option (only needed if AI_EMBEDDING_PROVIDER=local on the
# Django side; the worker itself does not embed, but listed for completeness if
# you colocate indexing here):
# sentence-transformers>=3.0
